<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lenovo.client">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.splashscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.registAndLogin" />
        <activity android:name=".view.verfication" />
        <activity android:name=".view.chooseMarket" />
        <activity android:name=".view.spinnerCity" />
        <activity android:name=".view.store" />
        <activity android:name=".view.home" />
        <activity android:name=".view.favourite" />
        <activity android:name=".view.continueToBuy" />
        <activity android:name=".view.sendBill"></activity>
    </application>

</manifest>